{
  "name": "ir-procedure-navigator-feedback-worker",
  "private": true,
  "version": "0.1.0",
  "description": "Files in-app feedback as GitHub issues. Deployed separately from the site.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260730.1",
    "typescript": "^5.7.3",
    "wrangler": "^4.28.0"
  }
}
